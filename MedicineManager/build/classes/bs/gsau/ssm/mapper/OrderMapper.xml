<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bs.gsau.ssm.mapper.OrderMapper">
	
	<!-- 查找已经购买的 -->
	<select id="findOrder" resultType="BOrder" parameterType="String">
		select * from b_order where username=#{username}
	</select>
	
</mapper>